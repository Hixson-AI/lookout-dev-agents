# PostgreSQL connection (uses shared agents_rag database from cybersecurity-hardening)
POSTGRES_HOST=localhost
POSTGRES_PORT=5435
POSTGRES_DB=agents_rag
POSTGRES_USER=agent_user
POSTGRES_PASSWORD=changeme

# OpenRouter for embeddings
OPENROUTER_API_KEY=sk-or-...
OPENROUTER_EMBEDDING_MODEL=openai/text-embedding-3-small

# Architecture repo path (relative to this agent)
ARCHITECTURE_REPO=../architecture
